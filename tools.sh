#!/usr/bin/bash

# Clone a remote repo
git clone URL

# Push changes to remote repo
git push

# Push to certain branch
git push -u origin <branch-name>

# Update remote changes on local machine
git pull